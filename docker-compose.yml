services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: soundprint
      POSTGRES_PASSWORD: soundprint
      POSTGRES_DB: soundprint
    ports:
      - "5432:5432"
    volumes:
      - soundprint_postgres_data:/var/lib/postgresql/data

volumes:
  soundprint_postgres_data:
